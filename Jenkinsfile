pipeline {
    agent { label 'master' }
    stages {
        stage('build') {
            steps {
              sh '''#!/bin/bash
                    echo "Build Task1Part1"
                   '''
            }
        }
    }
}
